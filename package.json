{
  "name": "h4c-monorepo",
  "private": true,
  "workspaces": ["web", "bot", "shared"],
  "scripts": {
    "install:all": "npm install && npm --prefix web install && npm --prefix bot install && npm --prefix shared install",
    "dev": "concurrently \"npm --prefix web run dev\" \"npm --prefix bot run dev\"",
    "build": "npm --prefix web run build && npm --prefix bot run build",
    "seed:badges": "node scripts/seed_badges.js",
    "lint": "node -e \"console.log('lint placeholder - add eslint if desired')\"",
    "typecheck": "node -e \"console.log('typecheck placeholder - Next handles TS build')\"",
    "test": "node tests/smoke.js && node tests/integration-award.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
