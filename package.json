{
  "name": "h4c-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "H4C Discord bot and web platform monorepo",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/h4c-monorepo.git"
  },
  "workspaces": [
    "shared",
    "bot",
    "web"
  ],
  "scripts": {
    "preinstall": "npm run setup:shared",
    "setup:shared": "cd shared && npm install",
    "postinstall": "npm run link:workspaces",
    "link:workspaces": "cd bot && npm link ../shared && cd ../web && npm link ../shared",
    
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:bot\"",
    "dev:web": "npm --prefix web run dev",
    "dev:bot": "npm --prefix bot run dev",

    "build": "npm run build:shared && npm run build:web && npm run build:bot",
    "build:shared": "echo 'Shared module ready'",
    "build:web": "npm --prefix web run build",
    "build:bot": "echo 'Bot ready'",

    "start": "concurrently \"npm run start:web\" \"npm run start:bot\"",
    "start:web": "npm --prefix web run start",
    "start:bot": "npm --prefix bot run start",

    "test": "npm run test:shared && npm run test:web && npm run test:bot",
    "test:shared": "npm --prefix shared test",
    "test:web": "npm --prefix web run test:jest",
    "test:bot": "npm --prefix bot test",

    "lint": "npm run lint:shared && npm run lint:web && npm run lint:bot",
    "lint:shared": "npm --prefix shared run lint",
    "lint:web": "npm --prefix web run lint",
    "lint:bot": "npm --prefix bot run lint",

    "clean": "npm run clean:deps && npm run clean:builds",
    "clean:deps": "rm -rf node_modules */node_modules",
    "clean:builds": "rm -rf */dist */.next */build",

    "seed:badges": "node scripts/seed_badges.js",

    "docker:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:prod": "docker-compose up -d",
    "docker:down": "docker-compose down",

    "setup": "npm ci && npm run link:workspaces",
    "install:all": "npm ci && cd shared && npm ci && cd ../bot && npm ci && cd ../web && npm ci && cd ..",

    "format": "prettier --write \"**/*.{js,ts,tsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,ts,tsx,json,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.18.0",
    "npm": ">=8.0.0"
  }
}
